<Window x:Class="MoneyRules.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login"
        Height="280"
        Width="360"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Email -->
        <TextBlock Text="Email:" FontSize="14" Margin="0,0,0,5"/>
        <TextBox x:Name="EmailTextBox"
                 Grid.Row="1"
                 Height="30"
                 FontSize="14"
                 Margin="0,0,0,15"/>

        <!-- Password -->
        <TextBlock Text="Пароль:" FontSize="14" Grid.Row="2" Margin="0,0,0,5"/>
        <PasswordBox x:Name="PasswordBox"
                     Grid.Row="3"
                     Height="30"
                     FontSize="14"
                     Margin="0,0,0,15"/>

        <!-- Login Button -->
        <Button x:Name="LoginButton"
                Grid.Row="3"
                Content="Увійти"
                Width="120"
                Margin="0,54,0,-33"
                HorizontalAlignment="Center"
                Click="LoginButton_Click"/>
    </Grid>
</Window>
